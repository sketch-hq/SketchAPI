{
    "name": "sketch-api",
    "version": "1.1.0",
    "author": "Sam Deane",
    "license": "MIT",
    "description": "Javascript API for Sketch",
    "main": "src_cjs/API.js",
    "scripts": {
        "prestart": "npm run clean",
        "start": "npm run compile && npm run deploy",
        "poststart": "rm -Rf build/tmp",
        
        "clean": "rm -Rf build && rm -Rf src_cjs && mkdir build && mkdir build/tmp",
        "compile": "tsc -p . && browserify src_cjs/API.js --outfile build/tmp/api.js && cat header.js build/tmp/api.js > build/api.js",
        "deploy": "cp build/api.js $npm_package_config_output",
        
        "watch": "watch 'npm start' src"
    },
    "devDependencies": {
        "browserify": "^14.4.0",
        "watch": "^1.0.2"
    },
    "dependencies": {}
}
